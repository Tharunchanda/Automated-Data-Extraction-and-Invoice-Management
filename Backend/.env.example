# Backend environment example
PORT=4000
# Toggle using Gemini/Vertex integration (if available)
USE_GEMINI=false
# If using Google Cloud/Vertex AI, provide path or set secrets as appropriate
# GCLOUD_SERVICE_ACCOUNT=/path/to/service-account.json
# GCLOUD_BUCKET=my-gcs-bucket
